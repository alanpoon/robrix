//! Webcam preview + 21-landmark overlay widget for the Robot tab.
//!
//! Structural stub for layer 1 — the full widget (DSL definition, NV12→RGBA
//! texture upload, Sdf2d landmark overlay) is delivered in a later layer.

#![allow(dead_code)]

use makepad_widgets::*;

/// Stub script_mod entry point. Real widget definition lives in a later
/// implementation layer.
pub fn script_mod(_vm: &mut ScriptVm) {}
